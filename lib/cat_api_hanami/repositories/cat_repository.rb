# frozen_string_literal: true

class CatRepository < Hanami::Repository
  # def all(_id)
  #   cats = cats.select(:id, :name)
  #   cats
  # rescue StandardError
  #   raise $ERROR_INFO
  # end

  # def find(id)
  #   cat = cats.where(id: id).first
  #   return cat if cat
  # rescue StandardError
  #   raise $ERROR_INFO
  # end

  # def create(name)
  #   cat = create(name: name)
  #   return cat if cat
  # rescue StandardError
  #   raise $ERROR_INFO
  # end
end
